### Using reduce recreate other array's methods listed below.

- [ ] .map
- [ ] .filter
- [ ] .every
- [ ] .some

```javascript
function mapFn(array, callback) {}

function filterFn(array, callback) {}

function everyFn(array, callback) {}

function someFn(array, callback) {}
```
